export enum SchedulerType {
  COSINE = 'cosine',
  LINEAR = 'linear',
  CONSTANT = 'constant',
}

export enum OptimizerType {
  ADAM = 'adam',
  ADAMW = 'adamw',
  SGD = 'sgd',
}
export enum TrainerType {
  STANDARD = 'Standard',
}
